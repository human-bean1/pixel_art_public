other cleaning methods:
#removes spaces     escaped = "".join(ch for ch in location if ch.isalnum())
#too liberal     escaped = location.replace(r'[^\w\s]|_', '')
#no     escaped = re.escape(location)

==
    # note: stretch goal, try "https://www.geeksforgeeks.org/how-to-find-longitude-and-latitude-for-a-list-of-regions-or-country-using-python/"


===
#     # Size of the image in pixels (size of original image)
#     # (This is not mandatory)
#     width, height = new_image.size
    
#     # Setting the points for cropped image
#     left = 0
#     top = height / 4
#     right = width
#     bottom = 3 * height / 4
 
#     # Cropped image of above dimension
#     # (It will not change original image)
#     new_image = new_image.crop((left, top, right, bottom))
#     new_satelliteIm = new_satelliteIm.crop((left, top, right, bottom))
===

print("Files and directories in '", path, "' :")
 
    # prints all files
    print(dir_list)